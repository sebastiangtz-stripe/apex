# Dual-Write Log

Append-only audit trail. Every `apply-proposals.py` run writes one entry.

_This file is workspace-local — it accumulates real merchant entries at runtime
and is excluded from template sync. The template ships it empty._
